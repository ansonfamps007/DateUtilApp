package com.date.app;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The main class. Starts the spring boot application.
 */
@SpringBootApplication
public class DateUtilAppApplication {

  /**
   * Main method that starts spring boot application.
   *
   * @param args the command line arguments
  */
  public static void main(String[] args) {
    SpringApplication.run(DateUtilAppApplication.class, args);
  }
}
